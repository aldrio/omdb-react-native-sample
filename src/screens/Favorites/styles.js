import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  list: {
    marginHorizontal: -12,
  },
  emptyBox: {
    minHeight: 150,
    justifyContent: 'center',
    alignItems: 'center',
  },
})
